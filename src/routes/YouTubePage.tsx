import { SourceListPage } from './sourceList/SourceListPage'

export default function YouTubePage() {
  return (
    <SourceListPage
      type="youtube"
      heroTitle="YouTube"
      heroDescription="See the latest uploads from your favourite channels with sections for Today, Yesterday, and earlier in the week."
    />
  )
}
