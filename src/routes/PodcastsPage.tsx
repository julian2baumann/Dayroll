import { SourceListPage } from './sourceList/SourceListPage'

export default function PodcastsPage() {
  return (
    <SourceListPage
      type="podcast"
      heroTitle="Podcasts"
      heroDescription="Catch up on podcast episodes grouped by day. Switch the range to keep your listening queue manageable."
    />
  )
}
