import { SourceListPage } from './sourceList/SourceListPage'

export default function NewsPage() {
  return (
    <SourceListPage
      type="news"
      heroTitle="News"
      heroDescription="Latest articles from your trusted sources. Summaries and Listen actions will appear where supported."
    />
  )
}
